# Colab-first dependency policy
# -----------------------------
# The E0 SETUP cell does an import-probe and pip-installs ONLY packages that
# are missing. On a standard Colab runtime that means just open-clip-torch
# (and sometimes einops/ftfy) - everything else ships preinstalled.
#
# NEVER pin exact old versions here: forcing 2024-era wheels onto a current
# Colab runtime downgrades compiled dependencies (numpy/pandas/pyarrow/...)
# and breaks the numeric ABI ("numpy.dtype size changed"). Floors only.
#
# This file is for local/non-Colab reproduction:
#   pip install -r requirements.txt

# Core (Colab preinstalled - listed with floors for local installs)
torch>=2.3
torchvision>=0.18
numpy>=1.26
pandas>=2.0
pyarrow>=14
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=10
tqdm>=4.66

# HuggingFace (ViLT for E6; Colab preinstalled)
transformers>=4.44
safetensors>=0.4

# CLIP backbone (usually the only real install on Colab)
open-clip-torch>=2.26
timm>=1.0

# Utilities
einops>=0.8
ftfy>=6.1
regex>=2023.0

# spaCy stays OPTIONAL (E9 entity extraction has a deterministic fallback).
# Do not install it in setup: older spaCy/Thinc wheels can downgrade NumPy.
