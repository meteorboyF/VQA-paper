# Pinned for Google Colab Pro+.
# Keep Colab's preinstalled PyTorch/CUDA stack.
# pip install -r requirements.txt

# ── Core deep learning ──────────────────────────────────────────────────────
# Use Colab's preinstalled torch/torchvision/torchaudio build.
# It is matched to the current Python/CUDA runtime; pinning old wheels can fail.

# ── HuggingFace ecosystem ───────────────────────────────────────────────────
transformers==4.44.2
tokenizers==0.19.1
accelerate==0.33.0
datasets==2.21.0
huggingface-hub==0.24.6
safetensors==0.4.4
# sentence-transformers is not used in the core pipeline.

# ── CLIP (OpenCLIP — used for ViT-B/32 loader) ──────────────────────────────
open-clip-torch==2.26.1

# ── Vision / image processing ───────────────────────────────────────────────
Pillow==10.4.0
timm==1.0.9

# ── Data / numerics ─────────────────────────────────────────────────────────
# Use Colab's preinstalled NumPy/Pandas/PyArrow/SciPy/Scikit-learn stack.
# These packages include compiled extensions; downgrading one without the
# others can produce "numpy.dtype size changed" binary incompatibility errors.

# ── Plotting ────────────────────────────────────────────────────────────────
# Use Colab's preinstalled Matplotlib/Seaborn unless local debugging needs pins.

# ── NLP (entity extraction for E9) ──────────────────────────────────────────
# spaCy is optional for E9 entity extraction. Do not install it in E0 setup:
# Colab already ships a compatible NLP stack, and forcing an older spaCy/Thinc
# build can downgrade NumPy and poison the live kernel. If spaCy is unavailable,
# src.grounding.extract_entity uses a deterministic fallback.

# ── Utilities ───────────────────────────────────────────────────────────────
tqdm==4.66.5
einops==0.8.0
ftfy==6.2.3
regex==2024.7.24

# ── Colab / Drive ───────────────────────────────────────────────────────────
# google-colab is pre-installed in Colab; listed here for documentation only
# google-colab==1.0.0
